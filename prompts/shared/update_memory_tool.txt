Update an existing entry in your long-term memory. Use it when new information belongs to a topic, person or job that already has an entry - to extend it, to correct it (the user corrects a fact, new information supersedes the old) or to merge overlapping entries into one (then delete the others with delete_memory). Reference the memory by the ID shown in brackets in your memory context. Provide the full text - it replaces the old content entirely, so carry over everything from the old entry that still holds. Keep it concise: the key facts, no embellishment or repetition, as bullet points where it fits.
