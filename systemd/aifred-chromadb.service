[Unit]
Description=AIfred ChromaDB + SearXNG (Docker)
After=docker.service
Requires=docker.service
PartOf=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=__PROJECT_DIR__/docker
# Der Docker-Binary-Pfad wird vom Installer (scripts/install-services.sh)
# per sed aus `command -v docker` aufgelöst — typisch /usr/bin/docker auf
# apt-Hosts, /snap/bin/docker bei Snap-Docker, /usr/local/bin/docker bei
# manuellem Install. systemd akzeptiert keinen Lookup über PATH bei ExecStart.
# SearXNG is the primary web-research backend, so it runs alongside ChromaDB.
ExecStart=__DOCKER_BIN__ compose up -d chromadb searxng
ExecStop=__DOCKER_BIN__ compose stop chromadb searxng
ExecReload=__DOCKER_BIN__ compose restart chromadb searxng
Restart=on-failure

[Install]
WantedBy=multi-user.target
